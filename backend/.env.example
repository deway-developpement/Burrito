JWT_SECRET=random-string-value # Secret key for JWT
JWT_EXPIRES_IN=30min # Token expiration time
JWT_REFRESH_EXPIRES_IN=30d # Token refresh expiration time
AUTH_MAX_ACTIVE_SESSIONS=10 # Maximum active refresh sessions per user
AUTH_REFRESH_REVOKED_RETENTION_DAYS=30 # Keep revoked/reuse-detected sessions before TTL cleanup
AUTH_REFRESH_IDEMPOTENCY_WINDOW_MS=1000 # Grace window for concurrent duplicate refresh rotation
DATABASE_USERNAME=db-user # Database username
DATABASE_PASSWORD=db-password # Database password
DATABASE_NAME=burrito # Database name
MONGODB_CONTAINER_NAME=burrito-mongo # MongoDB container name
MONGODB_PORT=27017 # MongoDB port
MONGODB_MODE=docker # Options: localhost, docker
REDIS_HOST=localhost # Redis host
REDIS_PORT=6379 # Redis port
REDIS_RETRY_ATTEMPTS=1000000 # Redis retry attempts for Nest Redis transport
REDIS_RETRY_DELAY_MS=1000 # Redis retry delay in milliseconds
OTEL_SDK_DISABLED=false # Disable OpenTelemetry SDK when set to true
OTEL_SERVICE_NAMESPACE=burrito # OpenTelemetry service namespace
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector.monitoring.svc.cluster.local:4318/v1/traces # OTLP traces endpoint
OTEL_EXPORTER_OTLP_HEADERS= # Optional OTLP headers, comma-separated key=value
OTEL_RESOURCE_DEPLOYMENT_ENVIRONMENT=production # Deployment environment tag
BURRITO_NAMESPACE=evaluation-system # Logical namespace attribute for telemetry
MAILHOG_CONTAINER_NAME=burrito-mailhog # MailHog container name
MAILHOG_SMTP_PORT=1025 # MailHog SMTP port
MAILHOG_WEB_PORT=8025 # MailHog web UI port
SMTP_HOST=mailhog # SMTP host (MailHog for local)
SMTP_PORT=1025 # SMTP port (MailHog for local)
SMTP_USER= # SMTP username
SMTP_PASS= # SMTP password
SMTP_FROM=no-reply@example.com # From address
NOTIFICATIONS_QUEUE_ENABLED=true # Enable BullMQ worker
NOTIFICATIONS_RETRY_ATTEMPTS=5 # Queue retry attempts
NOTIFICATIONS_BACKOFF_MS=1000 # Queue backoff ms
NOTIFICATIONS_DIRECT_RETRY_ATTEMPTS=2 # Direct send retry attempts
NOTIFICATIONS_REMINDER_ONLY_PENDING=true # Reminders only for pending recipients
WEB_APP_URL=http://localhost:3000 # Web app base URL
EMAIL_VERIFICATION_URL_BASE=http://localhost:3000/verify-email # Email verification base URL
EMAIL_VERIFICATION_TTL_HOURS=24 # Email verification token TTL
HUGGINGFACE_HUB_TOKEN=your-huggingface-token # Legacy token kept for compatibility

ANALYTICS_ENABLE_INTELLIGENCE=false # Enable async intelligence enrichment pipeline
ANALYTICS_INTELLIGENCE_PENDING_TIMEOUT_MS=300000 # Pending timeout before marking FAILED
ANALYTICS_INTELLIGENCE_REQUEST_STREAM=analytics:intelligence:request:v1 # Request stream name
ANALYTICS_INTELLIGENCE_RESULT_STREAM=analytics:intelligence:result:v1 # Result stream name
ANALYTICS_INTELLIGENCE_DLQ_STREAM=analytics:intelligence:dlq:v1 # Dead-letter stream name
ANALYTICS_INTELLIGENCE_CONSUMER_GROUP=analytics-ms # Redis stream consumer group for analytics-ms
ANALYTICS_INTELLIGENCE_RESULT_BATCH_SIZE=20 # Number of result events consumed per poll
ANALYTICS_INTELLIGENCE_RESULT_BLOCK_MS=2000 # Blocking read duration on result stream

INTELLIGENCE_FN_MODEL_VERSION=speed-lexicon-v1 # Reported model/version for intelligence function
INTELLIGENCE_SENTIMENT_LEXICON_PATH=assets/sentiment_words.json # Lexicon path loaded by Rust function at startup
